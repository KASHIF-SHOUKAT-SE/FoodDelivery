import MenuSidebar from './MenuSidebar';
import MenuCard from "./MenuICard";

const SidebarSection = () => {
  return (
    <div>
      <MenuSidebar />
      <MenuCard />
    </div>
  );
};

export default SidebarSection;